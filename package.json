{
    "private": true,
    "scripts": {
        "dev": "mix -- --stats-children",
        "prod": "mix --production",
        "test": "echo 'not yet implemented'; exit 0;",
        "watch": "mix watch --hot -- --stats-children"
    },
    "engines": {
        "node": ">=v16.13"
    },
    "devDependencies": {
        "laravel-mix": "^6.0.39",
        "resolve-url-loader": "^4.0.0",
        "sass": "^1.44.0",
        "sass-loader": "^12.4.0"
    }
}
