{
    "name": "web-opt",
    "version": "2.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "test": "node --test \"tests-js/**/*.test.*\"",
        "test:dist": "node --test \"tests-dist/**/*.test.js\"",
        "verify": "npm test && npm run build && npm run test:dist"
    },
    "engines": {
        "node": ">=24"
    },
    "devDependencies": {
        "jsdom": "^29.1.1",
        "sass": "^1.101.3",
        "vite": "^8.1.5"
    }
}
